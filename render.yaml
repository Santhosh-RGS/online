services:
  - type: web
    name: onlineclip
    env: node
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: npm start --prefix backend
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 5000
